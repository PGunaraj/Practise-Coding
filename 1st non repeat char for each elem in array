def firstNonRepeatChar(s):
	d={}
	q=[]
	ans=[]
	for ch in s:
		q.append(ch)
		if ch in d.keys():
			d[ch]+=1
		else:
			d[ch]=1
		while len(q)>0:
			if d[q[0]]>1:
				q.pop(0)
			else:
				ans.append(q[0])
				break
		if len(q)==0:
			ans.append(str(-1))
	print(ans)

firstNonRepeatChar("aabcb")
